/* ════════════════════════════════════════════════════════════════
   FIREBASE CONFIG  —  EDIT THIS FILE (or generate it with setup.html)

   Paste the firebaseConfig object you copied from the Firebase console
   (Project settings ⚙ → General → Your apps → </> Web app) below,
   replacing the placeholder values.

   While apiKey is left as "PASTE_YOUR_CONFIG_HERE", the dashboard runs
   in single-user localStorage mode (no sharing). Once filled in and
   deployed, all visitors share the same Firestore backend.

   This is a PUBLIC web config — it's safe to commit. Your data is
   protected by firestore.rules + Anonymous Auth, not by hiding this.
   ════════════════════════════════════════════════════════════════ */
window.FIREBASE_CONFIG = {
  apiKey: "PASTE_YOUR_CONFIG_HERE",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
